# 万物有层 V5 · 实战交付流程升级

V5 把多期实战中反复出现的问题沉淀为稳定工作流，重点提升知识短视频从选题、解释到发布收尾的完整性。

## 主要升级

- **完整系列结构**：固化“知识钩子 → 因果解释 → Skill 方法证明 → 三步复用 → 分享/CTA”结构，避免开场先讲工具、结尾缺少复用说明。
- **三层因果，多镜头表达**：三层仍是知识主线，但不再限制为三个镜头；30–60 秒内容可按语义拆成约 7–9 个单一职责的视觉节拍。
- **最终旁白驱动时间线**：旁白文件一旦替换，必须重新转写、重建语义锚点并复检画面，不再沿用旧时间线估算同步。
- **动态镜头职责更清楚**：生成视频用于真实动作、环境变化和镜头运动；精确剖面、内部机理、箭头、标签与中文排版由 SVG、HTML、3D 或其他可控方式完成。
- **Agnes 公链直调与分级验收**：默认通过公开素材直链调用 API，不打开网页；生成结果按完整采用、裁切采用、合成采用或拒绝记录，局部可用不再算整条通过。
- **交付闭环**：封面比例、平台安全区、逐字同步、声音和编码后关键帧进入同一验收流程，保留可追溯的版本化成片。

## 使用方式

调用方式不变：获取 Skill，将 `knowledge-paper-theater` 放入 Agent 的 Skills 目录，然后输入一个知识问题。Skill 会根据当前可用的生图、视频、矢量、旁白和渲染能力选择可完成路径。

```text
使用 knowledge-paper-theater，把“圆珠笔尖的小球为什么能写出墨”做成一条竖屏知识短片。
```
